import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app loads', (WidgetTester tester) async {
    // Placeholder test - the app widget is tested visually via flutter run
  });
}
